group = rootProject.group
version = rootProject.version

dependencies {
    implementation project(':sdk')

}

shadowJar {
    configurations = [project.configurations.runtimeClasspath]

    relocate 'bans.lib', 'io.bans.plugin.libs.sponge' // Relocate this lib
    minimize()
}